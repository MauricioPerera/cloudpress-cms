import { json, requireAuthor } from "../../../_shared.js";
export async function onRequestPost({request,env,params}){const author=await requireAuthor(request,env);if(!author)return json({error:"Se requiere rol autor"},403);const id=Number(params.id);if(!Number.isInteger(id)||id<1)return json({error:"Solicitud inválida"},400);const item=await env.DB.prepare("SELECT status,trashed_from_status FROM content_items WHERE id=? AND author_id=?").bind(id,author.id).first();if(!item||item.status!=="trash")return json({error:"Contenido no encontrado en la papelera"},404);const status=item.trashed_from_status==='published'?'published':'draft';await env.DB.prepare("UPDATE content_items SET status=?,trashed_from_status=NULL,trashed_at=NULL,updated_at=? WHERE id=? AND author_id=?").bind(status,new Date().toISOString(),id,author.id).run();return json({ok:true,status})}
export async function onRequestDelete({request,env,params}){const author=await requireAuthor(request,env);if(!author)return json({error:"Se requiere rol autor"},403);const id=Number(params.id);if(!Number.isInteger(id)||id<1)return json({error:"Solicitud inválida"},400);const r=await env.DB.prepare("DELETE FROM content_items WHERE id=? AND author_id=? AND status='trash'").bind(id,author.id).run();return r.meta.changes?json({ok:true,deleted:true}):json({error:"Contenido no encontrado en la papelera"},404)}
