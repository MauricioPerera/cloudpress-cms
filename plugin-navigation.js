(()=>{window.addEventListener('load',async()=>{try{const response=await fetch('/api/admin/plugin-schema');if(!response.ok)return;const schema=await response.json();for(const type of schema.contentTypes){const button=[...document.querySelectorAll('aside .nav.sub')].find(item=>item.textContent.trim()===type.label);if(!button)continue;const replacement=button.cloneNode(true);replacement.onclick=()=>{document.querySelectorAll('.nav').forEach(item=>item.classList.toggle('active',item===replacement));document.querySelector('#title').textContent=type.label;document.querySelector('#desc').textContent='Tipo de entrada declarado por un plugin activo.';document.querySelector('#work').src=`/admin.html?contentType=${encodeURIComponent(type.type_id)}`};button.replaceWith(replacement)}}catch{}})})();
